# 核心依赖
opencv-python>=4.8.0
onnxruntime>=1.15.0
numpy>=1.24.0
pyyaml>=6.0
psutil>=5.9.0

# Web界面
flask>=2.3.0
flask-cors>=4.0.0
flask-sock>=0.6.0
gunicorn>=21.0.0

# 通信
paho-mqtt>=1.6.0
requests>=2.31.0

# 硬件控制（可选）
# RPi.GPIO>=0.7.1; platform_machine=='armv7l' or platform_machine=='aarch64'
# gpiozero>=1.6.3; platform_machine=='armv7l' or platform_machine=='aarch64'

# 开发调试
pytest>=7.4.0
